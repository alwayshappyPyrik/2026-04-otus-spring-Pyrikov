spring:

  flyway:
    enabled: true
    locations: classpath:db/migration
    encoding: UTF-8

  shell:
    interactive:
      enabled: true

  datasource:
    url: jdbc:h2:mem:maindb

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true